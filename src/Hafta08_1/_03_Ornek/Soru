    // Bir Üniversitede öğrencilere ders kaydı yapılacaktır.
    // 1- Lesson : fields : name, saat (1-10 arasında değer alıyor).
    // 2- Student: fields : name, maxSaat, dersleri adında derslerini listesini tutacak
          bir liste.

    // 3- 3 adet ders oluşturunuz.
    // 4- 1 adet öğrenci tanımlayınız alabileceği maxCredit(maxSaat) si 10 olsun.
    // 5- Bu öğrencinin ders listesine açılmış dersleri ekleyiniz.
    //    ders eklerken max alabileceği Saati geçmemeli, geçerse
    //    uyarı versin, alabileceğiniz max Saati doldu şeklinde.
    // 6- Universite kurallarını yazdıran 2 maddelik bir not yazdıran metod ekleyiniz. En başta
    //    yazdırınız.

          Java101   3 saat
          Algr101   5 saat
          Math101   6 saat

          main()
          {
            Lesson d1 = new Lesson(); d1.name = "Java101"; d1.saat = 3;
            ogr1.dersEkle(d1);
          }