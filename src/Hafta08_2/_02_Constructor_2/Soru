    // Book class yazınız. fields : name, publishYear(yayın yılı), author
    // 3 adet Constructor ekleyiniz.
    // Kitapların özelliklerini yazdıran bir metod ekleyiniz.
    // 3 adet kitabı farklı constructor la oluşturup, mainde yazdırınız.
        libraryMain

        Book()

        Book(name, publishYear, author)

        Book(name)